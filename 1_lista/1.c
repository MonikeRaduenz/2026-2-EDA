#include <stdio.h>
#include <stdlib.h>

#define N 5

int main(){
    int v[N], k;
    printf("Insira um valor para k:\n");
    scanf("%i", &k);
    for(int i; i < N; i++){
        int soma = 0;
        soma = v[N] + soma;
    }
}
